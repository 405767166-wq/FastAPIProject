# 会议纪要智能转写系统 — 架构说明（对应当前代码）

> 定位：本文把三份材料对齐——`接口文档.md`（总规格）、`版本规划.md`（V1/V2 切分执行蓝本）、`backend/`（实际代码）。
> 状态图例：✅ = 骨架已落地可用 · 🔶 = V1 核心待实现 · ⬜ = V2 待实现

---

## 1. 一句话架构

**FastAPI 接收上传 → 任务入队 → 后台 worker 异步消费（分块→转写→词频→总结）→ 结果落存储 → REST 查询**；
生产者（API）与消费者（worker）通过**队列解耦**，全部存储/引擎都走**接口抽象 + 配置开关**，因此"V1 纯原生无外部依赖"与"V2 全功能"是同一套代码的两个配置档。

```
┌─ 请求方（V1: curl / .http · V2: Vue3 前端 via Nginx）─────────┐
│  上传/列表/详情/词频/总结/删除    （V2: /ws 实时进度）          │
└───────────────┬──────────────────────────────────────────────┘
                │ HTTP JSON（统一响应体 {code, message, data}）
┌───────────────▼────────── backend/app（FastAPI :8000）────────┐
│  api/   REST 路由（M3）          main.py 装配 + 生命周期        │
│        │                                                      │
│        ▼ 上传后 push(task)                                     │
│  queue/  TaskQueue 抽象 ◄── native_queue(asyncio.Queue)✅M4    │
│        │                     redis_queue(List) ⬜V2            │
│        ▼ pop(task)                                             │
│  worker.py 消费循环（M11）🔶                                  │
│        │ 分块→ASR→词频→总结→落库                              │
│        ▼                                                      │
│  store/  MeetingStore 抽象 ◄── memory(dict)✅M10 · mysql ⬜V2  │
│  freq/analyzer(Counter+jieba)🔶M5   asr/(mock)✅M7            │
│  summary/(template)🔶M8   chunk/(single)✅M6 · ffmpeg ⬜V2     │
└───────────────┬──────────────────────────────────────────────┘
                ▼
   内存态（重启即失）   （V2: 服务器 MySQL / Redis / PubSub→WS）
```

---

## 2. 代码结构 → 职责 → 状态

```
FastAPIProject/
├─ 启动服务器.bat           双击启动（先清端口再起服务，推荐日常用）
├─ pyproject.toml          依赖清单 + pytest 配置（Python ≥3.12）
├─ backend/
│  ├─ run.py               一键启动入口：修正 sys.path → uvicorn.run("app.main:app")
│  ├─ .env.example         配置模板（V1 默认值即可运行）
│  ├─ app/
│  │  ├─ main.py           FastAPI 装配；lifespan 启动/停止 worker；ApiError 全局处理器
│  │  ├─ config.py         Settings 读 backend/.env；提供 storage/queue 后端判定
│  │  ├─ responses.py      统一响应体 ok()/err()
│  │  ├─ errors.py         ErrCode(0/40001/40401/40901/50001/50002) + ApiError
│  │  ├─ worker.py         Worker：while True: pop → _handle（🔶 换成真实管线）
│  │  ├─ queue/            M4：base(抽象) + native_queue(asyncio.Queue)✅
│  │  ├─ store/            M10：base(协议) + memory(dict，含词频)✅
│  │  ├─ freq/             M5：analyzer 占位 🔶（jieba+Counter+停用词+TopN）
│  │  ├─ asr/              M7：base(ASREngine) + mock(示例文本+延时)✅
│  │  ├─ summary/          M8：base(Summarizer) + template 占位 🔶
│  │  ├─ chunk/            M6：base(Chunker) + single(整文件单块)✅
│  │  └─ api/              M3：health ✅ + meetings（列表/详情/词频/总结/删除✅；上传🔶）
│  └─ tests/               pytest：冒烟 4 例已绿 ✅
├─ 接口文档.md              总规格（§4 模块 / §5 API / §6 表结构…）
├─ 版本规划.md              V1/V2 切分 + 验收 + 里程碑
└─ test_main.http          REST 冒烟请求脚本（IntelliJ/PyCharm 可直接发）
```

---

## 3. 一次上传的完整时序（V1 目标形态）

| 步 | 动作 | 代码位置 | 状态 |
|----|------|----------|------|
| 1 | `POST /api/meetings`（multipart: file, title?） | `api/meetings.py: create_meeting` | 🔶 |
| 2 | 校验扩展名/大小 → 落盘 `uploads/{meeting_id}/raw.*` | 同上 + `config.upload_dir` | 🔶 |
| 3 | 生成 uuid meeting_id → `store.create_meeting(status=queued)` | `store/memory.py` ✅ | ✅ |
| 4 | `queue.push(task)` 入队，立即返回 200 | `queue/native_queue.py` ✅ | ✅ |
| 5 | worker `pop()` 取出 → 置 processing | `worker.py` ✅框架 | 🔶 内芯 |
| 6 | `chunk/` 切块（V1 单块）→ 记录 chunk_count | `chunk/single.py` ✅ | 🔶 接线 |
| 7 | 逐块 `asr/mock.transcribe()` → 拼全文 | `asr/mock.py` ✅ | 🔶 接线 |
| 8 | `freq/analyzer` jieba 分词 + Counter → TopN → `save_words` | `freq/analyzer.py` | 🔶 |
| 9 | `summary/template` 生成纪要（is_mock=true） | `summary/template.py` | 🔶 |
| 10 | `store.update(status=completed, progress=100, stage=done)` | `store/memory.py` ✅ | 🔶 接线 |
| 失败 | 任意步异常 → status=failed + error_message（不静默丢失） | `worker.py` ✅ | ✅ |

查询侧（已可用 ✅）：`GET /api/meetings` 分页列表 / `GET /{id}` 详情（含 status/progress/stage，V1 看进度就看它）/ `GET /{id}/words` / `GET /{id}/summary` / `DELETE /{id}`，全部经 `MeetingStore` 抽象，错误走统一响应体。

---

## 4. 双版本开关总表（一套代码两档）

| 维度 | 抽象接口 | V1（当前默认） | V2（配置即切换） | 切换依据（.env） |
|------|----------|----------------|------------------|------------------|
| 队列 M4 | `TaskQueue` | asyncio.Queue | Redis List | `REDIS_URL` 非空 |
| 存储 M10 | `MeetingStore` | dict 内存（重启失） | MySQL（服务器） | `MYSQL_HOST` 非空 |
| 转写 M7 | `ASREngine` | mock | 百度/讯飞/whisper | `ASR_PROVIDER` |
| 总结 M8 | `Summarizer` | 模板降级 | DeepSeek | `DEEPSEEK_API_KEY` 非空 |
| 分块 M6 | `Chunker` | 整文件单块 | ffmpeg 60s/块 | ffmpeg 可用 |
| 进度 | —（V1 查详情） | 详情字段 | P2.4 轮询 + WS | V2 路由 |
| 前端/网关 | — | 无 | Vue3 + Nginx | V2 目录 |

> 注：目前 `main.py` 的 `build_store()/build_queue()` 为 V1 固定实现；V2 时改为按 `settings` 选择实现——**接口已就位，切换点已预留**。

---

## 5. 横切约定（全部已生效 ✅）

- **统一响应体**：`{code, message, data}`；业务异常 = 抛 `ApiError(code, message, http_status)` → `main.py` 全局处理器转 JSON（404→`40401` 等，见 `errors.py`）。
- **状态机**：`status ∈ queued|processing|completed|failed`，`progress 0-100`，`stage ∈ queued|chunking|asr|freq|summary|done` —— 三字段由 worker 联动维护。
- **配置**：全部读 `backend/.env`（模板 `.env.example`），代码零硬编码。
- **错误不静默**：worker 每任务 try/except → `error_message` 落库。

---

## 6. 运行与验证速查

| 场景 | 命令 |
|------|------|
| 日常启动 | 双击 `启动服务器.bat`（或 `python backend/run.py`） |
| 开发直启 | `.venv\Scripts\python -m uvicorn app.main:app --app-dir backend --reload --port 8000` |
| 测试 | `.venv\Scripts\python -m pytest -q` |
| 文档/调试 | 浏览器 `http://127.0.0.1:8000/docs`（Swagger） |
| 冒烟 | PyCharm 打开 `test_main.http` 点发送，或 curl `/api/health` |

---

## 7. 当前进度与下一步

- ✅ **已落地（骨架）**：包结构、配置/错误/响应基建、队列与存储真实实现、各引擎抽象、health + meetings 只读接口、4 个 pytest、启动脚本、V0 清理。
- 🔶 **V1 核心（下一里程碑）**：上传落盘入队 → worker `_handle` 真实管线（single chunk → mock ASR → jieba 词频 → 模板总结）→ 完成态闭环，并把上传 501 占位替换为真实现。
- ⬜ **V2**：服务器 MySQL/Redis、实时进度（P2.4+WS）、ffmpeg、真实 ASR/DeepSeek、前端、Nginx —— 全部有预留接口，按 `版本规划.md` §8 执行。
