# 会议纪要智能转写系统 — V1 / V2 双版本交付规划

> 本文件是**执行蓝本**（怎么分两步做完）；`接口文档.md` 是**总规格**（系统最终长什么样）。
> 切分决策（已与项目方确认）：**版本一（V1）= 基本功能**，**版本二（V2）= 所有功能**；
> 其中 **实时进度、前端、Nginx 服务器部署只属于 V2**。
> 存储决策：V1 用**内存态存储（不连 MySQL）**；V1 实现**全部 REST 接口**；两版本在**同一仓库内分阶段演进**（V2 = V1 全量 + 扩展，git 打 `v1` / `v2` 里程碑）。

---

## 1. 版本定义（一句话）

| 版本 | 定义 | 一句话 |
|------|------|--------|
| **V1 基本功能** | 纯后端核心闭环 | 「上传 → 原生队列 → Mock 转写 → jieba+Counter 词频 → 模板总结 → 内存存储 → 全 REST 可查可删」，curl 即可验收；**无前端、无 MySQL/Redis、无实时进度推送、无 Nginx** |
| **V2 所有功能** | 完整系统 | V1 全量 + 服务器 MySQL 持久化 + Redis 扩展队列/词频/PubSub + ffmpeg 分块 + 真实 ASR + DeepSeek 总结 + **实时进度（轮询+WS）** + **Vue3 前端** + **Nginx 部署** |

**共用原则**：两版本共用同一套代码与模块抽象，差异由 `.env` 配置开关 + 文件存在性决定；V1 的文件集合 ⊆ V2 的文件集合，不允许"V2 改了 V1 的行为"。

---

## 2. 模块归属表（对照 接口文档.md §4）

| 编号 | 模块 | V1 | V2 | V1 里做什么 | V2 额外做什么 |
|------|------|----|----|-------------|---------------|
| M1 | 前端 UI | — | ✅ | 不做 | Vue3+Vite+axios+ECharts 词云，上传/进度/历史/详情四页 |
| M2 | API 网关 | — | ✅ | 不做 | Nginx 静态托管 dist + 反代 `/api`、`/ws` |
| M3 | API 服务层 | ✅ | ✅ | 全 REST（P2.1/2.2/2.3/2.5/2.6/2.7/health），统一响应体 | 增加 P2.4 进度轮询 + WS 路由 |
| M4 | 任务队列 | ✅ | ✅ | **原生 `asyncio.Queue`（考核点）**，单测只测它 | Redis List（LPUSH/BRPOP）可选切换 |
| M5 | 词频哈希 | ✅ | ✅ | **`dict`+`Counter`+jieba（考核点）**，停用词过滤 + Top-N | Redis Hash（HINCRBY）可选切换 |
| M6 | 分块 | ⚠️ 降级 | ✅ | 无 ffmpeg：整文件单块（`chunk_count=1`），接口与进度模型保持一致 | ffmpeg 按时长真实切块（60s/块） |
| M7 | 语音转写 | ✅ | ✅ | **MockEngine**：内置示例文本 + 可观测延时，演示全链路 | 百度录音文件识别（异步 task_id+轮询）/ 讯飞 / 阿里 / whisper 适配，`ASR_PROVIDER` 切换 |
| M8 | 智能总结 | ✅ | ✅ | **模板降级**：摘取全文前 N 字 + 高频词占位，置 `summary_is_mock=true` | DeepSeek API 结构化纪要 + 前置 `clean_text` 纠错去口语 |
| M9 | 实时进度 | — | ✅ | 不推不轮询；状态经 P2.3 详情接口查看（status/progress/stage 字段） | P2.4 轮询保底 + WS-1 `/ws/meetings/{id}/progress`，Redis Pub/Sub 或进程内广播 |
| M10 | 持久化 | ⚠️ 内存 | ✅ | **MemoryStore**（dict，重启即失，兜底模式） | 服务器 MySQL + SQLAlchemy 落库，幂等唯一索引 |
| M11 | 消息串联 | ✅ | ✅ | 生产者→`asyncio.Queue`→worker→内存存储，失败记录 error_message | Redis 版串联 + Pub/Sub 进度 + 幂等落库，worker 校验取消 |

> M6 在 V1 给"单块降级"而不是空实现，是为了让进度分母（chunk_count）、stage 状态机从 V1 起就完整，V2 只替换分块器实现、不动业务代码。

---

## 3. API 归属表（对照 接口文档.md §5）

| 接口 | 路径 | V1 | V2 | 说明 |
|------|------|----|----|------|
| P2.1 上传 | `POST /api/meetings` | ✅ | ✅ | multipart；校验→落盘→生成 meeting_id→建内存记录(queued)→入队 |
| P2.2 列表 | `GET /api/meetings` | ✅ | ✅ | V1 分页筛选基于内存态数据 |
| P2.3 详情 | `GET /api/meetings/{id}` | ✅ | ✅ | 含 status/progress/stage；**V1 查看进度的唯一途径** |
| P2.4 进度轮询 | `GET /api/meetings/{id}/progress` | — | ✅ | 实时进度，属 V2 |
| P2.5 词频 | `GET /api/meetings/{id}/words` | ✅ | ✅ | 未到词频阶段返回 `40901` |
| P2.6 总结 | `GET /api/meetings/{id}/summary` | ✅ | ✅ | V1 恒为 `is_mock=true` |
| P2.7 删除 | `DELETE /api/meetings/{id}` | ✅ | ✅ | V1：删内存记录；在途任务由 worker 校验跳过写库 |
| WS-1 实时进度 | `WS /ws/meetings/{id}/progress` | — | ✅ | 实时进度，属 V2 |
| 健康检查 | `GET /api/health` | ✅ | ✅ | V1：`mode:"native", asr:"mock", mysql:false` |

**V1 状态查看方式**：上传后客户端轮询 `GET /api/meetings/{id}`（详情接口），观察 `status/progress/stage` 变化即可 —— 这与"实时进度（推送/进度页）属 V2"不冲突，V1 不做任何推送与专用进度通道。

---

## 4. 运行模式与 .env 差异

| 配置 | V1 | V2 |
|------|----|----|
| `REDIS_URL` | 留空 | `redis://[:密码@]<服务器>:6379/0` |
| `MYSQL_HOST/PORT/USER/PASSWORD/DB` | 不配置 | 服务器地址 + 账号（`meeting_minutes` 库） |
| `ASR_PROVIDER` | `mock` | `baidu`（缺 key 自动落 mock）/ 可切讯飞、阿里 |
| `DEEPSEEK_API_KEY` | 空 | 真实 key（缺失时模板降级并置 mock 标记） |
| 存储 | `STORE=memory`（默认且唯一） | `STORE=mysql`（可切回 memory 兜底） |
| 进度 | 无专用接口 | P2.4 轮询 + WS 推送（Pub/Sub 或进程内广播） |
| 分块 | 整文件单块 | ffmpeg 60s/块 |

---

## 5. V1 验收标准（无任何外部依赖，本机可全跑）

1. `pytest` 全绿：队列入队/消费、Counter 词频 Top-N、上传→处理→完成的整链路（mock）。
2. curl 冒烟：上传任意音频文件 → 轮询详情至 `completed` → 查词频（Top 词非空）→ 查总结（`is_mock=true`）→ 删除 → 列表为空。
3. 长文件/坏文件：非音频返回 `400` + `40001`；任务失败置 `failed` 且 `error_message` 非空、不静默丢失。
4. 重启进程后历史清空 —— 符合 V1 内存态设计（文档注明即可）。
5. 不依赖：MySQL ✗ / Redis ✗ / 任何 key ✗ / ffmpeg ✗ / 前端 ✗ / Nginx ✗。

## 6. V2 验收标准

1. V1 全部用例仍绿（回归）。
2. 服务器 MySQL：上传→完成→**重启后端后历史仍可查**（列表/详情/词频从 MySQL 读）；`word_frequency` 唯一索引幂等。
3. 实时进度：前端进度页进度条实时推进（WS 优先、断线自动回退 P2.4 轮询）。
4. Redis 模式：队列走 List、词频走 Hash、进度走 Pub/Sub，`/api/health` 显示 `mode:"redis"`。
5. 真实链路（有 key 时）：百度 ASR 出真实文本、DeepSeek 出结构化纪要（无 key 自动降级不报错）。
6. Nginx：浏览器访问 `http://localhost` 进入前端；`/api`、`/ws` 反代正常，SPA 路由回退正常。
7. ffmpeg 分块：长音频 `chunk_count>1`，逐块进度递增。

---

## 7. 目录结构（单仓库，V1 文件集 ⊆ V2）

```
FastAPIProject/
├─ backend/
│  ├─ app/
│  │  ├─ main.py              # FastAPI 装配 + lifespan（启停 worker）
│  │  ├─ config.py            # .env 读取（pydantic-settings）
│  │  ├─ api/
│  │  │  ├─ meetings.py       # P2.1/P2.2/P2.3/P2.5/P2.6/P2.7
│  │  │  ├─ progress.py       # P2.4（V2）
│  │  │  ├─ ws_progress.py    # WS-1（V2）
│  │  │  └─ health.py         # /api/health
│  │  ├─ queue/               # M4：base.py + native_queue.py(V1) + redis_queue.py(V2)
│  │  ├─ freq/                # M5：analyzer.py（jieba+Counter+停用词）
│  │  ├─ asr/                 # M7：base.py + mock.py(V1) + baidu.py(V2)
│  │  ├─ summary/             # M8：base.py + template.py(V1) + deepseek.py(V2)
│  │  ├─ chunk/               # M6：base.py + single.py(V1) + ffmpeg.py(V2)
│  │  ├─ store/               # M10：base.py + memory.py(V1) + mysql.py(V2)
│  │  ├─ worker.py            # M11：消费循环
│  │  └─ progress/            # M9（V2）：pubsub + ws manager
│  ├─ tests/                  # pytest：队列/词频/全流程单测（V1 考核点）
│  └─ .env.example
├─ frontend/                  # V2：Vue3 + Vite + axios + ECharts
├─ deploy/nginx.conf          # V2
├─ tools/                     # V2：nginx / ffmpeg（可选）
├─ uploads/                   # 运行期文件（gitignore；实际在 backend/uploads）
├─ pyproject.toml
└─ 接口文档.md / 版本规划.md / test_main.http
```

---

## 8. 执行顺序与里程碑

### 阶段 V1（目标：无外部依赖跑通考核点 + 全 REST）
1. **技术基线修正**：解释器定 **Python 3.12**（本机 `py -3.12` 已装），用 3.12 重建 `.venv`；`pyproject.toml` 改 `requires-python = ">=3.12"`；补依赖（`python-multipart`、`jieba`、`pydantic-settings`、`pytest`、`pytest-asyncio`、`httpx`）。—— ⚠️ 与文档 Python 3.12 对齐，避开 3.15 beta 的 wheel 风险。
2. 目录重构：`main.py` 迁入 `backend/app` 包结构；删除 V0 Hello World。
3. 核心数据结构：M4 原生队列、M5 词频、M6 单块降级、M11 worker 消费循环、M10 MemoryStore、统一响应体 + 错误码。
4. 全 REST 路由（§3 V1 列）+ Mock ASR + 模板总结 + 落盘校验。
5. pytest 单测 + curl 冒烟脚本 → **git tag `v1`**。

### 阶段 V2（目标：所有功能）
6. 服务器 MySQL：建库建表（§6 规格）→ MySQLStore + SQLAlchemy + 幂等落库（需你提供连接信息 + 建表授权）。
7. 实时进度：P2.4 轮询 → WS 连接管理 →（可选 Redis Pub/Sub）。
8. 真实能力接入：ffmpeg 分块、真实 ASR、DeepSeek（均带无 key/无 ffmpeg 降级）。
9. Redis 扩展：List 队列 + Hash 词频 + Pub/Sub（`REDIS_URL` 配好自动启用）。
10. 前端 Vue3 四页 + ECharts → Nginx 部署 → 端到端联调 → **git tag `v2`**。

---

## 9. 待项目方提供 / 确认（按需解锁）

| 项 | 哪个版本需要 | 状态 |
|----|--------------|------|
| 确认 V1 解释器改 3.12 并允许重建 venv | V1 开工前 | 待确认 |
| 服务器 MySQL：IP/端口/账号/密码 + `meeting_minutes` 建库授权 | V2 步 6 | 待提供 |
| 服务器 Redis：IP/端口/密码 + 本机 IP 防火墙白名单 | V2 步 7-9 | 待提供 |
| ffmpeg（本机或服务器） | V2 步 8 | 待确认 |
| 真实 ASR / DeepSeek key | V2 步 8（可选） | 可后补，缺省自动降级 |
