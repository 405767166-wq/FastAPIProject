# inspectionProfiles — 代码检查配置目录

PyCharm 的代码检查（Inspection）Profile 配置目录。

## 本级每个文件说明

| 文件 | 说明 |
|------|------|
| `profiles_settings.xml` | 检查 Profile 设置：`USE_PROJECT_PROFILE=false`（不启用项目级共享检查配置，使用 IDE 默认检查设置）、版本号 `1.0`。 |
