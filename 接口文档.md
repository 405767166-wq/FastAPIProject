# 会议纪要智能转写系统 — 项目接口与模块设计文档

> 版本：V2 Final 定稿版（对应方案定稿）
> 适用场景：本机 Windows 部署的教学/演示项目（含作业考核点与扩展加分点）
> 更新日期：随开发推进维护

---

## 1. 项目概述

**一句话定义**：用户上传会议录音，系统经「分块 → 语音转文字 → 中文分词词频统计 → DeepSeek 总结」生成结构化会议纪要，全程可查看实时进度，并支持历史记录回查。

**核心业务价值**：
- 长录音异步转写（不阻塞接口）
- 词频统计洞察会议主题
- AI 纪要总结
- 历史数据持久化与前端回查

---

## 2. 技术选型定稿（V2 Final，不再变更）

| 决策项 | 最终选择 | 说明 |
|--------|----------|------|
| 前端 | Vue3 + Vite + axios | SPA，另配 vue-router、ECharts（词频可视化） |
| 部署 | 本机 Windows + Nginx | 静态托管 `dist/` + 反向代理 `/api`、`/ws` |
| 后端 | Python 3.12 + FastAPI + Uvicorn | 异步接口层 + 后台 worker（同进程） |
| 队列 | **Python 原生 `asyncio.Queue`（考核点）** | 单进程内生产者→消费者解耦 |
| 哈希表 | **Python 原生 `dict` / `Counter`（考核点）** + jieba 分词 | 中文按词统计，而非按字 |
| Redis（扩展） | List 当队列、Hash 当词频表、Pub/Sub 推进度 | 加分项：持久化 + 跨进程 + 实时推送 |
| MySQL | 会议记录 + 词频持久化 | 供前端历史列表回查（8.0，库名 `meeting_minutes`） |
| 语音转文字 | 百度录音文件识别（无 key 时模拟降级） | 可配置切换讯飞/阿里通义听悟（精度更高） |
| 智能总结 | DeepSeek API（无 key 时模板降级） | 总结前可加一步 LLM 纠错，提升词频与文本质量 |
| 中文分词 | jieba | 词频统计前置依赖 |

**分层原则**：
- **主链路（考核点）**：不依赖 Redis / MySQL / 任何 key，纯 Python 原生跑通全部核心逻辑。
- **扩展层（加分项）**：Redis 队列/哈希/PubSub、MySQL 持久化、真实语音与总结 API，全部做成「配置开关 + 接口抽象」，可插拔。

---

## 3. 总体架构与数据流

### 3.1 架构图

```
┌────────────────────────────── 浏览器 (Vue3 SPA) ─────────────────────────────┐
│  上传页 │ 进度页 │ 历史列表页 │ 详情页(词频/总结)                                │
└──────────────┬──────────────────────────────────────────────┬───────────────┘
               │ HTTP (axios)                                  │ WebSocket(实时进度)
┌──────────────▼──────────────────────────────────────────────▼───────────────┐
│                          Nginx（本机 80/自定义端口）                          │
│      静态托管 frontend/dist  +  反向代理 /api → :8000  /ws → :8000           │
└──────────────┬──────────────────────────────────────────────────────────────┘
               │
┌──────────────▼────────────── FastAPI (Uvicorn :8000) ───────────────────────┐
│  A. API 层: 上传 / 查询 / 历史 / 进度 / 词频 / 总结 / 删除                    │
│  B. Worker 层(后台任务, 与 API 同进程):                                      │
│     消费队列 → 分块 → 逐块转写 → jieba分词+Counter → DeepSeek总结 → 写MySQL  │
│  C. 核心数据结构: asyncio.Queue + dict/Counter（考核点，全程原生可用）          │
│  D. 扩展适配层: Redis(List/Hash/PubSub) + 语音/总结 SDK 封装 + 模拟降级        │
└──────┬──────────────────────┬──────────────────────────────┬───────────────┘
       │ (原生模式：内存直通)   │ (扩展模式)                    │
       ▼                       ▼                              ▼
  内存态进度/结果          Redis(队列/词频/进度通道)        MySQL 8.0
  (重启即失, 兜底模式)      (扩展) meeting_minutes 库
                                                          ├ meeting_records
                                                          └ word_frequency
```

### 3.2 主数据流（上传 → 落库）

```
1. 前端 multipart 上传录音  →  POST /api/meetings
2. FastAPI 校验音频 → 落盘 → 生成全局唯一 meeting_id → 建 DB 记录(status=queued)
3. 任务入队：
    原生模式：asyncio.Queue.put(task)
    扩展模式：Redis LPUSH meeting:queue <task_json>  （Redis 开启后自动启用）
4. worker 消费：
    取任务 → 更新 status=processing
        → 分块（ffmpeg 按时长/大小切）
        → 逐块提交语音转写：
             有 key → 百度录音文件识别（异步 task_id + 轮询结果）
             无 key → 模拟引擎（内置示例文本 / 可选本地 faster-whisper 兜底）
        → 每完成一块：progress 推进 + （扩展模式）Pub/Sub publish 进度
        → 全文拼接 → jieba 分词 → Counter 统计词频
        → DeepSeek 生成总结（无 key → 模板降级）
        → 写 MySQL：转录全文 / 词频表 / 总结 / status=completed
5. 前端：
    上传后立即轮询或订阅实时进度 → 完成后展示 转写全文 + 词云 + 总结
6. 历史回查：GET /api/meetings 从 MySQL 分页读取
```

> 可靠性要点（前期分析结论，落地必须遵守）：
> - `meeting_id` 在 MySQL 建**唯一索引**，消费者写库保证**幂等**（重复消费不产生脏数据）。
> - 队列负责「在途任务」，MySQL 负责「最终落库」，**不要把 MySQL 当队列轮询**。
> - worker 写库失败要 try/catch + 重试 + 记录 error_message，任务不静默丢失。

---

## 4. 模块划分：功能与实现途径（核心章节）

### 4.0 模块总览

| 编号 | 模块 | 功能 | 实现途径（主/扩展） | 考核点 |
|------|------|------|---------------------|--------|
| M1 | 前端 UI | 上传、进度、历史、详情可视化 | Vue3 + Vite + axios + ECharts | 否 |
| M2 | API 网关 | 静态托管 + 反代 | Nginx（本机） | 否 |
| M3 | API 服务层 | REST 接口、校验、统一响应 | FastAPI + Pydantic | 否 |
| M4 | 任务队列 | 接收任务、缓冲、解耦 | `asyncio.Queue` / Redis List | **是** |
| M5 | 词频哈希表 | 中文分词 + 频次统计 | `dict`+`Counter`+jieba / Redis Hash | **是** |
| M6 | 分块 | 长音频切片、逐块进度 | ffmpeg（命令行封装） | 是 |
| M7 | 语音转写 | 音频→文本 | 百度 SDK / 模拟引擎 / 可换讯飞阿里 | 否 |
| M8 | 智能总结 | 纪要生成 + 纠错 | DeepSeek API / 模板降级 | 否 |
| M9 | 实时进度 | 任务状态推送 | 轮询接口（主） + Redis Pub/Sub→WS（扩展） | 否 |
| M10 | 持久化 | 会议与词频落库回查 | MySQL + SQLAlchemy | 否 |
| M11 | 消息串联 | 生产者-消费者、进度、幂等落库 | 队列+worker+PubSub 组合 | 是(结合 M4) |

### 4.1 M1 前端 UI 模块

**功能**
- 上传会议录音（文件选择、标题、上传中/后跳转进度页）
- 实时进度展示（阶段 + 百分比 + 当前转写片段预览）
- 历史列表（分页、状态筛选）
- 详情页：转写全文、Top-N 词频（词云/柱状图）、AI 总结

**实现途径**
- `npm create vite@latest`（Vue3 + JS/TS 模板）
- 路由：vue-router（`/upload`、`/meetings`、`/meetings/:id`）
- HTTP：axios 实例，`baseURL: '/api'`（走 Nginx 同源，避免跨域）
- 可视化：ECharts（`echarts-wordcloud` 词云）
- 进度：优先 WebSocket（扩展模式）；不可用时降级 `setInterval` 轮询 `/api/meetings/{id}/progress`

**接口消费清单**：`P2.1` 上传、`P2.2` 列表、`P2.3` 详情、`P2.4` 进度、`P2.5` 词频、`P2.6` 总结、`P2.7` 删除、WS 通道。

### 4.2 M2 API 网关（Nginx）

**功能**
- 托管前端构建产物 `frontend/dist`
- 反向代理 `/api/*`、`/ws` → `http://127.0.0.1:8000`
- 解决同源问题（前端与后端同域名，天然无 CORS 烦恼）

**实现途径**
- Windows 版 Nginx 解压至 `tools/nginx/`，自定义 `conf/nginx.conf`
- 关键配置（详见 §11）：`location / { root dist }`、`location /api/ { proxy_pass http://127.0.0.1:8000; }`、`location /ws { proxy_http_version 1.1; Upgrade/Connection 头 }`

### 4.3 M3 API 服务层（FastAPI）

**功能**
- 定义全部 REST 路由（见 §5）
- 上传文件校验（扩展名/大小/时长上限）与落盘
- 参数校验与响应统一封装（Pydantic 模型）
- 生命周期管理：启动时拉起 worker 任务，关闭时优雅停止

**实现途径**
- FastAPI + uvicorn（`app.main:app`）
- Pydantic v2 `BaseModel` 定义请求/响应 schema
- `python-multipart` 支持上传
- 统一响应体：`{ "code": 0, "message": "ok", "data": ... }`（见 §5.1）

### 4.4 M4 任务队列模块（**考核点之一**）

**功能**
- 承接 API 层提交的任务（`task = {meeting_id, file_path, ...}`）
- 缓冲突发上传，避免瞬时打满 worker
- 生产者（API 层）与消费者（worker）解耦

**实现途径（双实现，抽象同一接口）**

| 模式 | 数据结构 | 操作 | 启用条件 |
|------|----------|------|----------|
| 原生（主/考核） | `asyncio.Queue` | `put_nowait` / `await get` | 默认 |
| Redis（扩展） | List | `LPUSH meeting:queue` / `BRPOP`（阻塞式消费） | `.env` 配好 Redis 即自动启用 |

**关键设计**
- 接口抽象：`class TaskQueue` 暴露 `push(task)` / `pop()`，内部按配置选择原生或 Redis 实现，业务代码不感知。
- 原生模式下与 FastAPI 同一事件循环：API 层 `put_nowait`，后台 worker 协程 `await get` 循环处理。
- 单元测试只测原生实现（考核点），不依赖 Redis。

### 4.5 M5 词频哈希表模块（**考核点之一**）

**功能**
- 对转写全文做中文分词并按词频累加
- 输出 Top-N 高频词供前端可视化与洞察

**实现途径（双实现）**

| 模式 | 数据结构 | 操作 | 说明 |
|------|----------|------|------|
| 原生（主/考核） | `dict[str,int]` / `collections.Counter` | `for w in jieba.cut(text): counter[w] += 1` | 过滤停用词 + 单字/无意义词后取 Top-N |
| Redis（扩展） | Hash | `HINCRBY meeting:{id}:words <word> 1` | 多 worker 并发累加原子安全 |

**关键设计**
- **分词是前置步骤**：中文必须 jieba 分词后再计数，否则按字统计无意义。
- 停用词表（`stopwords.txt`）过滤「的/了/我们/嗯/啊」等口语与虚词。
- 归一化：统一小写、去标点，数字/专名可后续加自定义词典优化。
- 原生 `Counter.most_common(n)` 完成 Top-N。

### 4.6 M6 分块模块

**功能**
- 将长录音按「时长或大小」切分为多个音频块
- 支撑：逐块转写（规避单次转写时长上限）、细粒度进度、可并发

**实现途径**
- 方案 A：`ffmpeg -ss S -t T -i input -acodec pcm/mp3 chunk.mp3`（推荐，需 ffmpeg 加入 PATH）
- 方案 B：`pydub`（底层仍是 ffmpeg）
- 块策略：默认每块 ≤ 60s（百度短音频友好）或 ≤ 10MB；切块数记入 `meeting_records.audio_chunk_count`，用于进度分母
- 产出：`uploads/{meeting_id}/chunks/chunk_0001.mp3 ...` 顺序列表

### 4.7 M7 语音转写模块

**功能**
- 音频块 → 文本
- 有真实 key 走厂商 API；无 key 时**模拟降级**，保证全链路无 key 可跑通

**实现途径**
- 主选：百度智能云「录音文件识别」（提交 `task_id` → 异步轮询结果），块级串行或小并发
- 无 key 模拟引擎：读取内置示例文本 / 按块名映射假文本，附带可观测的"处理中"延时，演示 UI 与进度
- 可选兜底（零成本真实转写）：本地 `faster-whisper`（CPU 小模型），作为「无 key 时第二级降级」
- 厂商适配器接口：`class ASREngine { transcribe(path) -> str }`，`BaiduEngine / MockEngine / WhisperEngine / (可选)讯飞、阿里通义听悟`
- 精度选型结论（已调研）：中文会议精度 讯飞、阿里通义听悟 第一梯队 > 火山引擎 > 腾讯/百度；海外 OpenAI/Azure 中文不占优且访问受限，不作主力。通过 `.env` 的 `ASR_PROVIDER` 一键切换，不影响其他模块。

### 4.8 M8 智能总结模块

**功能**
- 对转写全文生成结构化会议纪要（主题/要点/结论/待办）
- 前置「纠错 + 去口语」二次处理，提升词频统计质量（前文分析结论）

**实现途径**
- DeepSeek Chat API（`deepseek-chat`），HTTP 直调或 `openai` SDK（兼容 base_url）
- 无 key / 调用失败 → **模板降级**：按固定格式摘取全文前 N 字 + 高频词占位，生成可读纪要并标记 `is_mock=true`
- Prompt 模板双用途：`summarize`（总结）与 `clean_text`（纠错去口语），成本低、收益大

### 4.9 M9 实时进度模块

**功能**
- 向前端推送：状态流转（queued→processing→completed/failed）+ 百分比 + 当前块文本

**实现途径（两档，接口统一）**

| 档位 | 机制 | 适用 |
|------|------|------|
| 基础档（默认，全模式可用） | worker 每完成一块更新 DB.progress；前端**轮询** `GET /api/meetings/{id}/progress` | 原生模式必选 |
| 增强档（扩展，Redis 模式） | worker `PUBLISH meeting:{id}:progress <json>`；后端维护 WS 连接，把 Pub/Sub 消息桥接到 `/ws/meetings/{id}/progress` | Redis 启用后自动开 |

- 无论哪档，前端统一走同一套「进度事件」结构（见 §5.4），轮询/WS 只是传输层差异。
- 轮询接口设计为**前端兼容基础模式**的保底手段（前期分析里明确：轮询是 mode-agnostic 的稳妥方案）。

### 4.10 M10 持久化模块（MySQL）

**功能**
- 会议记录 + 词频落库
- 支撑前端历史列表、详情回查、任务状态追踪

**实现途径**
- SQLAlchemy 2.0（async）或同步 + PyMySQL 驱动；连接串从 `.env` 读
- 库：`meeting_minutes`；表与字段见 §6
- 关键约束：`word_frequency(meeting_id, word)` 唯一索引；`meeting_records.meeting_id` 唯一索引（幂等）
- 事务边界：worker 写「全文 + 词频 + 总结 + 状态」尽量一次事务，或用唯一键 + upsert 容忍重试

### 4.11 M11 消息串联（队列 → 处理 → MySQL）

**功能**
- 用消息组件把「接收 → 排队 → 异步处理 → 落库」整条链串起来（前期讨论结论落地）
- 生产者（API）不关心消费者（worker）与存储细节

**实现途径**
- 原生：`asyncio.Queue` 单进程内串联 + worker 协程写 MySQL
- 扩展：Redis List（队列）+ worker 独立消费 + **Pub/Sub 推实时进度**，consumer 写 MySQL
- 幂等：见 M10 唯一索引 + `INSERT ... ON DUPLICATE KEY UPDATE`
- 失败：`error_message` 记录 + 指数退避重试（≤3 次）→ 仍失败置 `failed`，前端可见，支持手动删除/重传
- 明确不做：**不用 MySQL 当队列轮询**；单机不引 RabbitMQ/Kafka（超纲）

---

## 5. REST API 接口规范

### 5.1 通用约定

- Base URL：直连 `http://127.0.0.1:8000`；经网关 `http://localhost/api`（前端统一用后者）
- Content-Type：JSON（上传为 `multipart/form-data`）
- 鉴权：本地演示不做；预留 `X-API-Key` 头位（后续可加）
- 统一响应体（所有业务接口）：

```jsonc
{
  "code": 0,        // 0=成功；非 0=业务错误码（见 5.10）
  "message": "ok",
  "data": { }       // 具体数据
}
```

- HTTP 状态码：`200` 成功 / `400` 参数错 / `404` 不存在 / `409` 冲突 / `500` 服务端错
- 任务状态机：`queued(0)` → `processing(1..99)` → `completed(100)` | `failed`
  （`progress` 字段 0-100，`status` 为枚举；两字段由 worker 联动维护）

### 5.2 P2.1 上传会议录音

- `POST /api/meetings`
- 请求：`multipart/form-data`
  | 字段 | 类型 | 必填 | 说明 |
  |------|------|------|------|
  | file | File | 是 | 音频（mp3/wav/m4a/aac，≤200MB） |
  | title | str | 否 | 会议标题，缺省用文件名 |
- 处理：校验 → 落盘 `uploads/{meeting_id}/raw.*` → 生成 `meeting_id`（uuid）→ 写记录(queued) → **入队** → 返回
- 成功响应 `data`：

```jsonc
{
  "meeting_id": "7f9c…ab3e",
  "title": "周会录音",
  "status": "queued",
  "progress": 0,
  "message": "已进入处理队列"
}
```

- 错误：`400` 非音频/超大小；`500` 落盘失败

### 5.3 P2.2 历史会议列表

- `GET /api/meetings`
- 查询参数（query）：`page=1&page_size=10&status=completed&keyword=周会`
- 成功响应 `data`：

```jsonc
{
  "total": 42,
  "page": 1,
  "page_size": 10,
  "items": [
    {
      "meeting_id": "…",
      "title": "周会录音",
      "status": "completed",
      "progress": 100,
      "duration_sec": 1830,
      "word_count": 2413,
      "summary_excerpt": "本次会议…",
      "created_at": "2025-01-01T10:00:00",
      "updated_at": "2025-01-01T10:05:00"
    }
  ]
}
```

### 5.4 P2.3 会议详情

- `GET /api/meetings/{meeting_id}`
- 成功响应 `data`（含转写全文与总结，字段为 `null` 表示尚未完成）：

```jsonc
{
  "meeting_id": "…",
  "title": "周会录音",
  "status": "completed",
  "progress": 100,
  "stage": "done",            // queued|chunking|asr|freq|summary|done
  "file_name": "周会.mp3",
  "duration_sec": 1830,
  "chunk_count": 31,
  "transcript": "（全文）",
  "summary": "（纪要）",
  "summary_is_mock": false,   // 是否降级模板生成
  "created_at": "…",
  "updated_at": "…",
  "error_message": null
}
```

### 5.5 P2.4 进度查询（轮询保底，原生/扩展通用）

- `GET /api/meetings/{meeting_id}/progress`
- 成功响应 `data`：

```jsonc
{
  "meeting_id": "…",
  "status": "processing",
  "progress": 47,            // 0-100
  "stage": "asr",
  "current_chunk": 15,
  "chunk_count": 31,
  "latest_text": "…（当前块已转写片段，可为空）",
  "updated_at": "…"
}
```

### 5.6 P2.5 词频查询

- `GET /api/meetings/{meeting_id}/words?top=20&min_freq=3`
- 成功响应 `data`：

```jsonc
{
  "meeting_id": "…",
  "total_words": 842,        // 去重词数
  "items": [
    { "word": "项目", "freq": 56 },
    { "word": "上线", "freq": 41 }
  ]
}
```

- 错误：`404` 会议不存在；`409` 任务尚未到词频阶段（status < completed 且无词频数据）

### 5.7 P2.6 总结查询

- `GET /api/meetings/{meeting_id}/summary`
- 成功响应 `data`：

```jsonc
{
  "meeting_id": "…",
  "summary": "（结构化纪要）",
  "is_mock": false,
  "model": "deepseek-chat"
}
```

### 5.8 P2.7 删除会议

- `DELETE /api/meetings/{meeting_id}`
- 行为：删除 DB 记录 + 词频 + 磁盘文件；若任务仍在队列/处理中，则标记取消（worker 侧校验 meeting_id 后跳过写库）
- 成功响应：`data: { "deleted": true }`

### 5.9 WS-1 实时进度通道（增强档）

- `WebSocket /ws/meetings/{meeting_id}/progress`
- 服务端推送（每完成一块 / 状态变化时）：

```jsonc
{ "type": "progress", "meeting_id": "…", "progress": 47, "stage": "asr",
  "current_chunk": 15, "chunk_count": 31, "latest_text": "…" }
{ "type": "done",    "meeting_id": "…", "progress": 100, "status": "completed" }
{ "type": "failed",  "meeting_id": "…", "error_message": "…" }
```

- 传输实现：worker（Redis 模式）`PUBLISH` → FastAPI WS 连接订阅后转发；原生模式则 worker 直接广播到进程内 WS 连接组（同样可用）
- 前端建议：WS 断开/超时自动回退到 P2.4 轮询

### 5.10 健康检查与错误码

- `GET /api/health` → `{ "code":0, "message":"ok", "data": { "status":"up", "mode":"native|redis", "asr":"mock|baidu|…", "mysql":true } }`（用于调试各扩展是否生效）

| code | message 含义 |
|------|--------------|
| 0 | 成功 |
| 40001 | 参数/文件校验失败 |
| 40401 | 会议不存在 |
| 40901 | 任务尚未进入该阶段 |
| 50001 | 内部错误（含 worker 写库失败） |
| 50002 | 第三方 API（语音/总结）失败（前端可提示"正在降级"） |

---

## 6. 数据库设计（MySQL：`meeting_minutes`）

### 6.1 表 `meeting_records`（会议主表）

| 字段 | 类型 | 约束/说明 |
|------|------|-----------|
| id | BIGINT | PK, AUTO_INCREMENT |
| meeting_id | VARCHAR(64) | **UNIQUE NOT NULL**（幂等键） |
| title | VARCHAR(255) | 默认取文件名 |
| file_name | VARCHAR(255) | 原始文件名 |
| file_path | VARCHAR(512) | 原始音频落盘路径 |
| file_size | BIGINT | 字节 |
| duration_sec | INT | 可空（未解析时 NULL） |
| chunk_count | INT | 分块数（进度分母） |
| status | VARCHAR(20) | queued/processing/completed/failed |
| progress | INT | 0-100 |
| stage | VARCHAR(20) | queued/chunking/asr/freq/summary/done |
| transcript | LONGTEXT | 转写全文 |
| summary | TEXT | AI 纪要（可空） |
| summary_is_mock | TINYINT | 是否模板降级 |
| error_message | VARCHAR(512) | 失败原因（可空） |
| created_at / updated_at | DATETIME | 默认 CURRENT_TIMESTAMP / ON UPDATE |

索引：`idx_status_created (status, created_at)` 供列表筛选。

### 6.2 表 `word_frequency`（词频明细）

| 字段 | 类型 | 约束/说明 |
|------|------|-----------|
| id | BIGINT | PK, AUTO_INCREMENT |
| meeting_id | VARCHAR(64) | 关联会议 |
| word | VARCHAR(64) | 词 |
| freq | INT | 频次 |
| created_at | DATETIME | |

索引：**`UNIQUE KEY uk_meeting_word (meeting_id, word)`**（幂等/upsert 用）；`KEY idx_meeting (meeting_id)`。

> 建表与连接需要 MySQL root 密码——**待你提供后再执行**（或你给只读/读写账号均可）。

---

## 7. 配置项（backend/.env）

```ini
# ---- 服务 ----
APP_HOST=127.0.0.1
APP_PORT=8000

# ---- MySQL（扩展：持久化）----
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=            # ← 需要你提供
MYSQL_DB=meeting_minutes

# ---- Redis（扩展：队列+哈希+PubSub），留空则走原生模式 ----
REDIS_URL=                 # 例：redis://127.0.0.1:6379/0

# ---- 语音转写 ----
ASR_PROVIDER=baidu         # baidu|mock|whisper|xfyun|ali
BAIDU_APP_ID=
BAIDU_API_KEY=
BAIDU_SECRET_KEY=
ASR_CHUNK_SECONDS=60

# ---- DeepSeek 总结 ----
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# ---- 存储目录 ----
UPLOAD_DIR=./uploads
```

降级规则（读配置决定，无需改代码）：
- `REDIS_URL` 空 → 原生队列/哈希；非空 → Redis 模式（启用 Pub/Sub 进度推送）
- `ASR_PROVIDER=baidu` 但缺 key → 自动落 `mock`
- `DEEPSEEK_API_KEY` 空 → 模板降级并置 `summary_is_mock=true`

---

## 8. 部署方案（本机 Windows + Nginx）

```
本机
├─ backend/          Python 3.12 + venv；uvicorn app.main:app --port 8000
├─ frontend/dist     npm run build 产物（Vue3 SPA）
├─ tools/nginx/      Windows 版 Nginx
├─ tools/ffmpeg/     ffmpeg（转写分块用，加入 PATH）
└─ tools/memurai/    或 Redis（扩展模式才需要；winget 安装，需管理员弹窗确认）
```

Nginx `nginx.conf` 要点：

```nginx
server {
    listen 80;
    server_name localhost;
    client_max_body_size 200m;          # 音频上传上限

    location / {
        root C:/CD/frontend/dist;
        index index.html;
        try_files $uri $uri/ /index.html;   # SPA history 路由回退
    }
    location /api/ {
        proxy_pass http://127.0.0.1:8000;   # 注意保留 /api 前缀反代
        proxy_set_header Host $host;
    }
    location /ws {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

启动顺序：MySQL →（可选 Redis）→ uvicorn（worker 随进程起）→ Nginx → 浏览器访问 `http://localhost`。

---

## 9. 开发顺序与验收点（沿用 10 步流程，前 3 步优先）

| 步 | 内容 | 依赖 | 验收 |
|----|------|------|------|
| 1 | `backend/`+`frontend/` 骨架；`requirements.txt`、`package.json`、目录结构、`.env.example` | 无 | 两边能启动/编译空壳 |
| 2 | **核心数据结构**：M4 队列、M5 词频(Counter+jieba)、M6 分块(ffmpeg)、M11 任务消费循环 + pytest 单测 | **不依赖 Redis/MySQL/key** | 单测全绿，模拟任务全流程跑通 |
| 3 | MySQL：建库建表、SQLAlchemy 模型、历史列表/详情/词频查询、worker 落库 + 幂等 | **MySQL root 密码（你提供）** | 上传→完成→历史可查 |
| 4 | 实时进度：轮询接口 → 前端进度页 → WebSocket | 3 | 进度条随 worker 推进 |
| 5 | 真实语音转写接入（百度/讯飞/阿里 + 模拟切换） | 语音 key（你可稍后申请） | 真实音频出文本 |
| 6 | DeepSeek 总结接入 + 纠错 + 降级标记 | DeepSeek key | 真实纪要 |
| 7 | Redis 扩展：List 队列 + Hash 词频 + Pub/Sub 推送切换 | Redis 安装确认（你） | 断 MySQL/Redis 对比可观测 |
| 8 | 前端完善：上传页、列表页、详情页词云/图表、错误态 | 3-6 | UI 走查 |
| 9 | Nginx 收尾：静态托管 + 反代 + SPA 回退 | 全部 | `http://localhost` 一键访问 |
| 10 | 联调 + 端到端 + 文档（本文档随开发更新） | 全部 | 演示脚本 + 考核点讲解 |

> 当前你处于「先分析、不动工」阶段，本文档仅沉淀设计，**尚未开始任何编码**。

---

## 10. 前期讨论结论速查（已并入上文）

1. **Redis 对比本地方案**：本机单进程演示用原生（快/简单/可测）；Redis 价值在持久化、跨进程/多 worker 共享、Pub/Sub 实时推送、原子累加与横向扩展；代价是额外依赖、网络序列化开销与运维。→ 定稿为「原生为主 + Redis 扩展」。
2. **消息串联 MySQL**：推荐「生产者 → 队列 → 消费者 → 写 MySQL」，队列管在途、MySQL 管最终态；强调 `meeting_id` 唯一索引幂等、勿把 MySQL 当队列轮询、失败重试不静默。
3. **语音转写选型**：中文会议精度 讯飞/阿里通义听悟 > 火山引擎 > 腾讯/百度；海外 API 中文不占优且访问受限；无 key 场景可用本地 faster-whisper 作真实降级。→ 模块做成 `ASR_PROVIDER` 可切换，默认百度 + mock。
4. **二次提升**：DeepSeek 在总结前先做「纠错 + 去口语」，可显著提升文本与词频质量，性价比高于更换更贵的 ASR。
```
